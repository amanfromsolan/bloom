## New
- Agent icons now show in the command palette (⌘T) and the Ctrl-Tab switcher.
- A terminal glyph marks idle tabs everywhere — it turns blue when something unrecognized is running.
- Custom folder icons in the sidebar, palette, and switcher.
- Double-click a tab's name in the terminal header to rename it inline — no more dialog.

## Improved
- Gemini's icon renders its gradients correctly.
- Long titles leave breathing room in the header instead of filling the whole strip.
- Bolder icon weights and tuned fades across the sidebar, palette, and switcher.
